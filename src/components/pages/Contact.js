import React, { Fragment } from "react";
import Contacts from "../../contact/Contacts";

const Contact = () => {
  return (
    <Fragment>
      <Contacts />
    </Fragment>
  );
};

export default Contact;
